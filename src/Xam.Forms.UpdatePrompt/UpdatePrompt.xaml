<?xml version="1.0" encoding="utf-8" ?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Xamarin.Forms.UpdatePrompt"
             x:Class="Xam.Forms.UpdatePrompt.UpdatePrompt"
             IsVisible="False">

    <Grid BackgroundColor="Black" Opacity="0.7" />
    <Frame x:Name="content" VerticalOptions="Center" HorizontalOptions="Center" BackgroundColor="White">
        <Grid>
            <Label x:Name="lbl" VerticalOptions="Center" HorizontalOptions="Center"  />
            <Button x:Name="btn" Clicked="OnButtonClicked" HorizontalOptions="Start" VerticalOptions="End"  />
            <Button Text="Cancel" Clicked="OnCancelClicked" HorizontalOptions="End" VerticalOptions="End" />
        </Grid>
    </Frame>

</Grid>
